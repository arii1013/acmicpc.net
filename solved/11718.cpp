#include <iostream>
#include <string>
using namespace std;
#define FAST_IO ios::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
#define ll long long

int main() {
    // FAST_IO

    // 입력
    while (true) {
        string s; getline(cin, s);
        if (s == "") break;
        cout << s << "\n";
    }
    return 0;
}